spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  sql:
    init:
      mode: always
security:
  jwt:
    token:
      secret-key: "mytopsecrettestingkey"
